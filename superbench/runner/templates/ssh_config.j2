{% for host in hostvars.values() | map(attribute='ansible_hostname') | sort %}
Host node{{ loop.index0 }} {{ host }}
    HostName {{ host }}
    Port {{ ssh_port }}
    IdentityFile /root/.ssh/key
    StrictHostKeyChecking no
{% endfor %}
