{% for host in hostvars.values() | map(attribute='ansible_default_ipv4.address') | sort %}
Host {{ host }}
    Port {{ ssh_port }}
    IdentityFile /root/.ssh/key
    StrictHostKeyChecking no
{% endfor %}
